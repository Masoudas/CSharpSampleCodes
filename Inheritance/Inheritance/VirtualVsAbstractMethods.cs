/**
- Abstract methods have no body, whereas virtual methods do.
- Virtual methods need not be overridden, whereas abstract methods must be overridden.

We can't actually say virtual abstract, we don't, because an abstract method is by definition
virtual.
*/
