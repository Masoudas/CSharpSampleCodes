/**
The remainder operator (%) applies to float point types as well as integers (as opposed to C++, 
where it only applies to integer).

The remainder returns remainder (a = bq +r, where -q<r<q). Hence, for example -3%2 = -1;
*/